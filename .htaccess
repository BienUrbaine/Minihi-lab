RewriteEngine On

# Adresse canonique de Minihi : HTTPS sans www.
RewriteCond %{HTTPS} !=on [OR]
RewriteCond %{HTTP_HOST} ^www\.minihi\.fr$ [NC]
RewriteRule ^ https://minihi.fr%{REQUEST_URI} [R=301,L]
